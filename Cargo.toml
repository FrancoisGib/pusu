[package]
name = "pusu"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0.100"
rand = "0.9.2"
serde = { version = "1.0", features = ["derive"] }

[features]
default = ["broker"]
broker = []
